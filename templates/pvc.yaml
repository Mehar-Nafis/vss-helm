{{- $fullname := include "vss.fullname" . }}
{{- $globalSC := .Values.persistence.storageClass }}

# ---------------------------------------------------------------------------
# vss-shared-media  (ReadWriteMany – shared /tmp across all GPU services)
# Requires a storage class that supports RWX (e.g. ODF CephFS, NFS).
# ---------------------------------------------------------------------------
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ $fullname }}-vss-shared-media
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "vss.labels" . | nindent 4 }}
spec:
  accessModes:
    - {{ .Values.persistence.vssSharedMedia.accessMode }}
  resources:
    requests:
      storage: {{ .Values.persistence.vssSharedMedia.size }}
  {{- with (.Values.persistence.vssSharedMedia.storageClass | default $globalSC) }}
  storageClassName: {{ . | quote }}
  {{- end }}
---
# ---------------------------------------------------------------------------
# via-ngc-model-cache  (ReadWriteMany – NGC model weights for via-server + cv-detector)
# ---------------------------------------------------------------------------
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ $fullname }}-via-ngc-model-cache
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "vss.labels" . | nindent 4 }}
spec:
  accessModes:
    - {{ .Values.persistence.viaNgcModelCache.accessMode }}
  resources:
    requests:
      storage: {{ .Values.persistence.viaNgcModelCache.size }}
  {{- with (.Values.persistence.viaNgcModelCache.storageClass | default $globalSC) }}
  storageClassName: {{ . | quote }}
  {{- end }}
---
# ---------------------------------------------------------------------------
# via-hf-cache  (ReadWriteMany – HuggingFace cache for via-server + cv-detector)
# ---------------------------------------------------------------------------
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ $fullname }}-via-hf-cache
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "vss.labels" . | nindent 4 }}
spec:
  accessModes:
    - {{ .Values.persistence.viaHfCache.accessMode }}
  resources:
    requests:
      storage: {{ .Values.persistence.viaHfCache.size }}
  {{- with (.Values.persistence.viaHfCache.storageClass | default $globalSC) }}
  storageClassName: {{ . | quote }}
  {{- end }}
---
# ---------------------------------------------------------------------------
# redis-data  (ReadWriteOnce)
# ---------------------------------------------------------------------------
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ $fullname }}-redis-data
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "vss.labels" . | nindent 4 }}
spec:
  accessModes:
    - {{ .Values.persistence.redisData.accessMode }}
  resources:
    requests:
      storage: {{ .Values.persistence.redisData.size }}
  {{- with (.Values.persistence.redisData.storageClass | default $globalSC) }}
  storageClassName: {{ . | quote }}
  {{- end }}
---
# ---------------------------------------------------------------------------
# vst-data  (ReadWriteOnce – VST database and logs)
# ---------------------------------------------------------------------------
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ $fullname }}-vst-data
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "vss.labels" . | nindent 4 }}
spec:
  accessModes:
    - {{ .Values.persistence.vstData.accessMode }}
  resources:
    requests:
      storage: {{ .Values.persistence.vstData.size }}
  {{- with (.Values.persistence.vstData.storageClass | default $globalSC) }}
  storageClassName: {{ . | quote }}
  {{- end }}
---
# ---------------------------------------------------------------------------
# vst-video  (ReadWriteOnce – VST video recordings)
# ---------------------------------------------------------------------------
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ $fullname }}-vst-video
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "vss.labels" . | nindent 4 }}
spec:
  accessModes:
    - {{ .Values.persistence.vstVideo.accessMode }}
  resources:
    requests:
      storage: {{ .Values.persistence.vstVideo.size }}
  {{- with (.Values.persistence.vstVideo.storageClass | default $globalSC) }}
  storageClassName: {{ . | quote }}
  {{- end }}
---
# ---------------------------------------------------------------------------
# nim-model-cache  (ReadWriteOnce – NIM LLM downloaded model weights)
# ---------------------------------------------------------------------------
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ $fullname }}-nim-model-cache
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "vss.labels" . | nindent 4 }}
spec:
  accessModes:
    - {{ .Values.persistence.nimModelCache.accessMode }}
  resources:
    requests:
      storage: {{ .Values.persistence.nimModelCache.size }}
  {{- with (.Values.persistence.nimModelCache.storageClass | default $globalSC) }}
  storageClassName: {{ . | quote }}
  {{- end }}
